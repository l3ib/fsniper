delay_time = 300
delay_repeats = 5 # in-line comments
watch { # what here?
	~/tmp {
		# comments by themselves
		/.*yats.*/ = rm %%

		*.txt = \
			converttoutf8.rb %%

		*.py = tar cfvz\   
			lolpylon.tar.gz %%

		image/* {
			handler = movetobackgrounds.rb %% # lol
			handler = mv %% ~/media/Pictures/
		}
	}

	~/download {
		* = mvtodl.pl %% "moved via sniper"
	}
}

numbers = [1, 3, 5, 7, 9, hello]
root 2 = 1.4142135623731 # an irrational!
pi = 3.14159265358979 # and another! wow!
